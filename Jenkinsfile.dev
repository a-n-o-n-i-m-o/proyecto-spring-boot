pipeline {
    agent any

    environment {
        name = 'demo-app'
        port = '8080:8080'
        projectPath = 'D:/proyecto-spring/demo'  // Ruta a tu proyecto Spring
    }

    stages {
        stage('Construir imagen') {
            steps {
                script {
                    dir(projectPath) {
                        bat "docker build -t ${name} ."
                    }
                }
            }
        }

        stage('Docker images') {
            steps {
                script {
                    dir(projectPath) {
                        bat 'docker images'
                    }
                }
            }
        }

        stage('Construir contenedor') {
            steps {
                script {
                    dir(projectPath) {
                        bat "docker run -it --publish ${port} ${name}"
                    }
                }
            }
        }

        stage('Docker container') {
            steps {
                script {
                    bat 'docker ps'
                }
            }
        }

        stage('Test') {
            steps {
                echo 'Exito'
            }
        }
    }
}

